// PRIMITIVE TYPES

  // Strings
  const name = 'John Doe';
  // Number
  const age = 45;
  // Boolean
  const hasKids = true;
  // Null
  const car = null;
  //Undefined
  let test;
  // Symbol
  const sym = Symbol();

// REFERENCE TYPES - Objects. Accessed by memory by reference

  // Array
  const hobbies = ['movies','music'];
  console.log(hobbies);

  // Object literal
  const adress = {
    city: 'Boston',
    state: 'MA'
  }
  console.log(adress);
  
  // Dates
  const today = new Date();
  console.log(today);

console.log(typeof adress);
